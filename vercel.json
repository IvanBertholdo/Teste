{
    "version": 2,
    "name": "my-api",
    "build": {
      "command": "npm install && npm run build"
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/index.html"
      }
    ],
    "framework": "nextjs"
  }